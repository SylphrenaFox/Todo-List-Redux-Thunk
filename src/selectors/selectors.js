export const selectTodos = (state) => state.todos;
export const selectTodoItem = (state) => state.todoItem;
export const selectSearchQuery = (state) => state.searchQuery;
export const selectIsSortTurnedOn = (state) => state.isSortTurnedOn;
export const selectIsCreating = (state) => state.isCreating;
export const selectIsLoading = (state) => state.isLoading;
export const selectIsDeleting = (state) => state.isDeleting;
export const selectIsUpdating = (state) => state.isUpdating;
